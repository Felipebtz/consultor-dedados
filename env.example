# Copie este arquivo para .env e preencha com suas credenciais (ex.: do projeto antigo maqtols-antigo). Não use credenciais de teste em produção.
# Configurações da API Omie
APP_KEY=your_app_key_here
APP_SECRET=your_app_secret_here
BASE_URL=https://app.omie.com.br/api/v1
TIMEOUT=30
MAX_RETRIES=3
RETRY_DELAY=1

# Extrato (obrigatório para a coleta de extrato não ficar zerada)
# Obtenha em: Omie > Financeiro > Contas Correntes > código (nCodCC) ou código integração (cCodIntCC)
# EXTRATO_CONTA_CORRENTE=123456
# EXTRATO_CONTA_CORRENTE_INTEGRACAO=CC001

# Google Cloud (BigQuery + GCS) - quando configurado, a coleta vai direto para o BigQuery (MySQL desativado)
GOOGLE_APPLICATION_CREDENTIALS=C:\xampp\htdocs\consultor-dedados\lille-422512-a12a0a3c757b.json
GCS_BUCKET=gscbronze
GCP_PROJECT_ID=lille-422512
BIGQUERY_DATASET=p79_maqtools

# Configurações do Banco de Dados MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=coleta_dados
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
